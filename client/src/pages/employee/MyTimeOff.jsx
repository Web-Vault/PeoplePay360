import { useEffect, useState } from 'react'
import { CalendarDays } from 'lucide-react'
import Button from '../../components/common/Button'
import { getMyTimeOff, requestTimeOff } from '../../services/selfService'
import { formatDate } from '../../utils/helpers'
export default function MyTimeOff() { const [data, setData] = useState({}); const [error, setError] = useState(''); const [saving, setSaving] = useState(false); const [form, setForm] = useState({ timeOffTypeId: '', startDate: '', endDate: '', reason: '' }); const load = () => getMyTimeOff().then((r) => { setData(r.data); setForm((f) => ({ ...f, timeOffTypeId: f.timeOffTypeId || r.data.types?.[0]?._id || '' })) }).catch((e) => setError(e.response?.data?.message || 'Could not load leave information')); useEffect(load, []); const submit = async (e) => { e.preventDefault(); setSaving(true); setError(''); try { await requestTimeOff(form); setForm((f) => ({ ...f, startDate: '', endDate: '', reason: '' })); load() } catch (err) { setError(err.response?.data?.message || 'Could not submit request') } finally { setSaving(false) } }; return <div className="space-y-6"><div><h1 className="text-2xl font-bold text-slate-900">My time off</h1><p className="mt-1 text-sm text-slate-500">Your leave balances and requests only.</p></div>{error && <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</p>}<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{data.allocations?.map((item) => <div key={item._id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><CalendarDays className="h-5 w-5 text-primary-600" /><p className="mt-3 font-semibold text-slate-900">{item.timeOffTypeId?.name}</p><p className="mt-1 text-2xl font-bold">{item.remainingDays}</p><p className="text-sm text-slate-500">days remaining</p></div>)}</div><form onSubmit={submit} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold text-slate-900">Request time off</h2><div className="mt-4 grid gap-3 sm:grid-cols-4"><select required value={form.timeOffTypeId} onChange={(e) => setForm({ ...form, timeOffTypeId: e.target.value })} className="rounded-lg border border-slate-300 px-3 py-2.5 text-sm">{data.types?.map((type) => <option key={type._id} value={type._id}>{type.name}</option>)}</select><input required type="date" value={form.startDate} onChange={(e) => setForm({ ...form, startDate: e.target.value })} className="rounded-lg border border-slate-300 px-3 py-2 text-sm" /><input required type="date" value={form.endDate} onChange={(e) => setForm({ ...form, endDate: e.target.value })} className="rounded-lg border border-slate-300 px-3 py-2 text-sm" /><Button type="submit" loading={saving}>Submit request</Button></div><input value={form.reason} onChange={(e) => setForm({ ...form, reason: e.target.value })} placeholder="Reason (optional)" className="mt-3 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm" /></form><section className="rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-5 font-semibold">Leave requests</div><div className="divide-y divide-slate-100">{data.requests?.map((item) => <div key={item._id} className="flex justify-between gap-4 p-5 text-sm"><div><p className="font-semibold">{item.timeOffTypeId?.name}</p><p className="mt-1 text-slate-500">{formatDate(item.startDate)} – {formatDate(item.endDate)} · {item.days} day(s)</p></div><span className="h-fit rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold">{item.status}</span></div>)}{!data.requests?.length && <p className="p-10 text-center text-sm text-slate-500">No leave requests yet.</p>}</div></section></div> }
