import { useEffect, useState } from 'react'
import { CalendarDays } from 'lucide-react'
import { getMyTimeOff } from '../../services/selfService'
import { formatDate } from '../../utils/helpers'
export default function MyTimeOff() { const [data, setData] = useState({}); const [error, setError] = useState(''); useEffect(() => { getMyTimeOff().then((r) => setData(r.data)).catch((e) => setError(e.response?.data?.message || 'Could not load leave information')) }, []); return <div className="space-y-6"><div><h1 className="text-2xl font-bold text-slate-900">My time off</h1><p className="mt-1 text-sm text-slate-500">Your leave balances and requests only.</p></div>{error && <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</p>}<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{data.allocations?.map((item) => <div key={item._id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><CalendarDays className="h-5 w-5 text-primary-600" /><p className="mt-3 font-semibold text-slate-900">{item.timeOffTypeId?.name}</p><p className="mt-1 text-2xl font-bold text-slate-900">{item.remainingDays}</p><p className="text-sm text-slate-500">days remaining</p></div>)}</div><section className="rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-5 font-semibold text-slate-900">Leave requests</div><div className="divide-y divide-slate-100">{data.requests?.map((item) => <div key={item._id} className="flex justify-between gap-4 p-5 text-sm"><div><p className="font-semibold text-slate-800">{item.timeOffTypeId?.name}</p><p className="mt-1 text-slate-500">{formatDate(item.startDate)} – {formatDate(item.endDate)} · {item.days} day(s)</p></div><span className="h-fit rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-600">{item.status}</span></div>)}{!data.requests?.length && <p className="p-10 text-center text-sm text-slate-500">No leave requests yet.</p>}</div></section></div> }
