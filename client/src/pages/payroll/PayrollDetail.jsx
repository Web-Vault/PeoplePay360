import { useEffect, useState } from 'react'
import { Link, useParams } from 'react-router-dom'
import { AlertTriangle, ArrowLeft, FileText } from 'lucide-react'
import { getPayrun } from '../../services/payrollService'
import { formatDate } from '../../utils/helpers'
const money = (n) => new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 }).format(n || 0)
export default function PayrollDetail() { const { id } = useParams(); const [run, setRun] = useState(null); const [error, setError] = useState(''); useEffect(() => { getPayrun(id).then((r) => setRun(r.data.payrun)).catch((e) => setError(e.response?.data?.message || 'Could not load payroll run')) }, [id]); if (error) return <p className="rounded-xl bg-red-50 p-4 text-red-700">{error}</p>; if (!run) return <p className="py-20 text-center text-sm text-slate-500">Loading payroll run…</p>; return <div className="space-y-6"><div><Link to="/payroll" className="inline-flex items-center gap-1 text-sm text-slate-500 hover:text-primary-600"><ArrowLeft className="h-4 w-4" />Payroll</Link><h1 className="mt-3 text-2xl font-bold text-slate-900">{run.name}</h1><p className="mt-1 text-sm text-slate-500">{formatDate(run.periodStart)} – {formatDate(run.periodEnd)}</p></div><section className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">Gross payroll</p><p className="mt-2 text-xl font-bold">{money(run.totalGross)}</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">Deductions</p><p className="mt-2 text-xl font-bold text-red-600">{money(run.totalDeductions)}</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">Net disbursement</p><p className="mt-2 text-xl font-bold text-emerald-700">{money(run.totalNet)}</p></div></section><section className="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-5 font-semibold text-slate-900">Payslips</div><table className="min-w-full divide-y divide-slate-100"><thead className="bg-slate-50 text-left text-xs font-semibold uppercase text-slate-500"><tr><th className="px-6 py-3">Employee</th><th className="px-6 py-3">Gross</th><th className="px-6 py-3">Deductions</th><th className="px-6 py-3">Overtime</th><th className="px-6 py-3">Net pay</th></tr></thead><tbody className="divide-y divide-slate-100">{run.payslips.map((slip) => <tr key={slip._id}><td className="px-6 py-4"><p className="font-semibold">{slip.userId?.name || slip.employeeSnapshot?.name}</p><p className="text-xs text-slate-500">{slip.payslipNumber}</p></td><td className="px-6 py-4 text-sm">{money(slip.grossSalary)}</td><td className="px-6 py-4 text-sm text-red-600">{money(slip.totalDeductions)}</td><td className="px-6 py-4 text-sm">{slip.overtimeHours || 0} hrs</td><td className="px-6 py-4 font-semibold text-emerald-700">{money(slip.netSalary)}</td></tr>)}</tbody></table></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="flex items-center gap-2 font-semibold text-slate-900"><AlertTriangle className="h-5 w-5 text-amber-600" />Audit findings</h2><div className="mt-4 space-y-3">{run.audits.map((audit) => <div key={audit._id} className="rounded-xl bg-slate-50 p-4 text-sm"><p className="font-semibold text-slate-800">{audit.userId?.name || 'Payroll-wide'} · {audit.severity}</p><p className="mt-1 text-slate-600">{audit.message}</p><p className="mt-1 text-xs text-slate-500">{audit.recommendation}</p></div>)}{!run.audits.length && <p className="text-sm text-slate-500">No audit findings.</p>}</div></section></div> }
